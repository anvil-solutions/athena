import PageTabBar from '../../components/page-tab-bar.js'

export default {
  name: 'calendar',
  template:
  `<page-tab-bar>
    <h2>Calendar</h2>
  </page-tab-bar>`,
  components: {
    PageTabBar
  }
}
